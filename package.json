{
  "name": "safe-clip-popclip",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "A local-first redaction extension for PopClip.",
  "scripts": {
    "sync": "node scripts/sync-packages.mjs --write",
    "sync:check": "node scripts/sync-packages.mjs --check",
    "build": "npm run sync:check && node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check src/redact.js && node --test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
