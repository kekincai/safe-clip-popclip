# Contributing

1. Create a focused branch.
2. Add a synthetic regression test before changing a redaction pattern.
3. Run `npm test`.
4. Explain false-positive and false-negative trade-offs in the pull request.

Never submit real credentials, personal information, financial data or private documents. Use reserved domains such as `example.test` and obviously synthetic values.
